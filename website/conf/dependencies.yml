# Application dependencies

require:
    - play
    - play -> scala 0.9.1
    - com.google.code.gson -> gson 2.1


repositories:
# This does not really work for replacing maven repo, all deps needs to be in contains section
# See http://stackoverflow.com/questions/7729119/how-can-i-configure-the-play-framework-to-use-my-artifactory-repository-for-all
